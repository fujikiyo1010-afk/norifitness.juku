# PR-S2 計画＋要素チェックリスト（挙動不変の往復削減） 2026-07-11

指示書: `_Cursor指示_速度改善_応急_2026-07-11.md` §4。Before基準: `PR-S1_before計測_2026-07-11.md`。
**原則: 画面の見た目・データは1ミリも変えない。変わるのは速度だけ。** 本書にない「ついで最適化」は禁止。

## スコープ（やる/やらない）
- やる: S2-1 直列→並列 / S2-2 親子2段→ネスト1本 / S2-3 署名URL一括（=改修不要・確認のみ済） / S2-4 画面外lazy。
- やらない: キャッシュ（=S3）/ force-dynamic除去 / JWT・RPC化 / ホーム全libのgetUser一括集約（影響大・下記Q2で相談）。

## 変更一覧（Before表の根拠つき・すべて挙動不変）

### A. /admin/daily — `listUsersWithAlerts()` 二重実行→1回（最大の削り代）
- 現状: `getDailyQueue`([daily.ts:42]) と `getDailyDetail`([daily.ts:288]) が各々フル実行（users全件＋関連5表＋auth.listUsers1000 を2回）。page.tsxで queue→detail は直列。
- 改修: page.tsx で `listUsersWithAlerts()` を1回だけ実行し、その結果を両関数へ引数で渡す（関数側は引数があればそれを使う・無ければ従来どおり自前取得＝後方互換）。
- 検証: キュー3グループの人数・並び／パネル4カードの値が改修前と一致。

### B. /meals — 独立クエリを Promise.all（9段→約4段）
- 現状: `getMealsForDate→signMealPhotos`(親子＋署名=依存) の後、`daily_feedbacks`([:47])・`goal_sheets`([:56])・`getDailyCondition`([:74])・`shouldAskYesterday`([:79])・`getActiveFoods`([:81])・`weekMeals`([:91]) を逐次await。
- 改修: 認証後、互いに独立な上記を Promise.all（allSettled/個別catchで「あるものだけ表示」のフォールバック維持）。meal取得→署名の鎖は内部順序を保ったまま、他と並列。
- 検証: meals/feedback/target/condition/foods/recordedDates の値が一致・画面スクショ同一。

### C. /workout/today — getUser集約＋演出3関数の並列化＋progress使い回し
- 現状: `getTodayWorkout` 内で getUser×3（本体＋getMyCurrentMenu＋getMyProgress・[logs.ts:72,74,89]）。完了演出で `getTodayActivity→getMyProgress→getRecordStreak` を逐次追加（[today/page.tsx:72,77,83]・progressは取得済みを再取得）。
- 改修: (1) getTodayWorkout 内の getUser を1回に集約し menu/progress取得へ引き回す。(2) 演出の3関数を Promise.all。(3) getTodayWorkout が既に持つ progress を演出側で使い回し（再取得を消す）。
- 検証: 読み取りのみ＝DB不変。翌日ゲート・完了演出・「明日は」ラベルが従来どおり（実クリックで確認）。

### D. ネスト取得（S2-2・親子2段→1本）
- `meal_logs→meal_log_items`（[meals/queries.ts]）／`user_workout_logs→user_workout_log_items`（[logs.ts]・履歴/today）／admin daily の meal→items・workout→items。
- 改修: Supabaseネストselect（`select("*, xxx_items(*)")`）で1往復に。
- 検証: **RLS不備でネスト先が空返りする事故があるため、dev で改修前後の「件数・値の一致」を必ず突合してから**（指示§S2-2）。

### E. 画面外 lazy（S2-4）
- 履歴一覧・日別詳細の下方など**ビューポート外**の写真に `loading="lazy"`。**初回表示画像には付けない**（LCP悪化回避）。

### F. ホーム後段の合流（低リスク分のみ）
- beta の `Promise.all(getRecordStreak,getTodayWorkout)`([page.tsx:137]) が上の Promise.all(12) の後に直列追加。→ 可能なら1つの Promise.all に合流。
- ※ getUser 15回重複の解消は全lib署名変更＝影響大。今回は**やらない**（Q2で相談）。

## 検証（各変更で全部・指示§S2）
1. 画面スクショが改修前後で同一。
2. 表示件数・値が DB の行と一致（特にネスト取得D）。
3. コンソールエラー0。
4. 実クリック（記録1件 投稿→表示→編集）で壊れない。
5. 計測表の該当ページを **live で before→after 再計測**（dev・同一アカウント・同一データ・各ページ3回中央値・条件不変）。クエリ数・直列段数・TTFB を数字で報告。

## PR分割（推奨）
安全のため**ページ単位の小PR連続**（A→B→C→D→E→F）。各PRで上記1〜5を回し、数字を添えて「判定をお願いします」で締める（自己合格宣言しない）。段1（挙動不変・事後報告可）だが、Dネスト取得だけは空返りリスクがあるため dev突合の数字を見せてから本番。

## 要素チェックリスト（報告時に 実装済/未/質問中 で更新）
- [ ] A /admin/daily listUsersWithAlerts 1回化 — 未
- [ ] B /meals 独立クエリ Promise.all（フォールバック維持）— 未
- [ ] C /workout/today getUser集約＋演出並列＋progress使い回し — 未
- [ ] D ネスト取得 4箇所（前後 件数・値 突合）— 未
- [ ] E 画面外 lazy（初回表示は除外）— 未
- [ ] F ホーム beta後段の Promise.all 合流 — 未
- [ ] 各PRで live before→after 再計測（3回中央値）を数字報告 — 未

## 質問（着手前に決めたい）
1. PR分割は「ページ単位の小PR連続（推奨）」でよいか。それとも1つの大PRにまとめるか。
2. ホームの getUser 15回重複の解消（全lib署名変更・影響大）は**今回やらない**方針でよいか（まず安全な束ね直しだけ）。
3. ネスト取得Dは「dev前後の件数・値突合の数字」を見せてから本番、で進めてよいか。
