-- =====================================================================
-- 2026-07-06: body_photos に軽量サムネのパスを追加 (P6 再設計)
-- =====================================================================
--
-- 毎日運用(1ヶ月〜30枚)でも一覧が重くならないよう、アップロード時に
-- フルサイズ(~1080px)とは別に 小さいサムネ(~240px/~15KB) も保存する。
--   - storage_path : フル画像 (拡大表示・ビフォーアフター用)
--   - thumb_path   : サムネ  (ギャラリー一覧用)  ※旧データは NULL=フルで代替
--
-- 一覧(/record/photos)はサムネのみ読み込み、拡大時だけフルを読む。

alter table public.body_photos add column if not exists thumb_path text;
