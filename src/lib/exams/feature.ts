/**
 * 試験(理解度テスト)の一時非表示フラグ(2026-07-14・全体公開の準備)。
 *
 * 背景: 試験ページは questions を correct_choice_id 付きでクライアントに送っており
 *   (ExamView が client 側で採点)、答えがネットワーク/JSから見えてしまう既知の
 *   セキュリティ債がある。ベータ少人数では黙認していたが、全体公開で全員に露出するため、
 *   公開準備として「入口(コース内のテスト行)」と「本体(/exam ページ・直URL)」の両方を塞ぐ。
 *
 * これは一時対応。正式修正=「採点をサーバー側で行い correct_choice_id をクライアントへ
 *   出さない」は公開後の宿題として起票済み(docs/09_全体公開/公開後_別工程計画.md)。
 *
 * 再開する時はここを true に戻すだけ(入口・本体が同時に復活する)。
 */
export const EXAMS_ENABLED = false;
