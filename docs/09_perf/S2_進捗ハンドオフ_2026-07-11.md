# S2 速度改善・進捗ハンドオフ（2026-07-11）

このファイル1枚で「今どこまで終わったか／残りは何か／どう検証したか」が分かるようにしてあります。
次に作業を継続する人（Claude/きよむさん）は、まずこれを読んでください。

## これは何か
米国Vercel(iad1)⇄東京DBの距離 × 直列往復 が遅さの主犯。**S2＝挙動を1ミリも変えず、往復を「並列化・親子ネスト・N+1解消」で減らす応急処置**。
- 指示書（最優先・この1枚を見る）: `_Cursor指示_速度改善_応急_2026-07-11.md`（ワークスペース直下）
- Before基準（きよむ採用）: `docs/09_perf/PR-S1_before計測_2026-07-11.md`
- 計画・チェックリスト: `docs/09_perf/PR-S2_計画_チェックリスト_2026-07-11.md`

## 合格線（きよむ確定）
**「今あるものが壊れていない」＝これが守れていればよい。** 見た目・データ・並び順は不変、変わるのは速度だけ。

## 検証方式（きよむ確定＝軽く）
- 安全な並べ替え（Promise.all化）＝**型/lint通過＋描画エラー0＋DBと件数・値が一致**の確認でよい（毎回のフル前後画像は不要）。
- **危険な「親子ネスト取得(D)」だけ**＝RLSで子が空落ちしないか、**dev前後の件数・値突合＋フル前後画像**で証明する（実施済）。
- **live TTFB（before→after）＝S2をdevへdeployした時点**で、norifitness-juku-dev・同一アカウント・同一データ・各ページ3回中央値・条件不変 で計測（まだ未実施）。

## 守りの3原則（違反=差し戻し・S3で特に重要）
1. 記録系（体重/食事/トレ/生活/FB/通知/しおり/目標シート）は**キャッシュ禁止**。マスタ系（food_table/メニュー原本/動画マスタ）のみ可。
2. ユーザー固有キャッシュキーに必ず user_id。
3. ログイン後ページを静的化しない（force-dynamic除去しない・キャッシュはデータ関数単位のみ）。
※ S3（キャッシュ）はまだ着手していない。S2完了後・きよむ実機OK後。

---

## 完了済み（このブランチ feat/p3-home-beta にコミット済・未デプロイ）

| 記号 | 対象 | 変更ファイル | 何をしたか | 検証 | 状態 |
|---|---|---|---|---|---|
| A | /admin/daily | `src/app/admin/daily/page.tsx`, `src/lib/admin/daily.ts` | `listUsersWithAlerts()` を1回だけ取得して queue/detail 両方へ渡す（2回フル全件スキャン→1回）。管理トップも同関数使用のため波及 | 描画OK・キュー/パネル/タグ不変・エラー0 | ★きよむ合格 |
| B | /meals | `src/app/meals/page.tsx` | 独立読み取り7本を1つの Promise.all（依存のmeals→署名は1単位・shouldAskYesterdayはtoday限定を維持） | before/after完全一致・データ通過 | ★きよむ合格 |
| C | /workout/today | `src/lib/workout/logs.ts`, `src/lib/workout/queries.ts`, `src/app/workout/today/page.tsx` | getTodayWorkout の getUser 3→1（getMyProgress/getMyCurrentMenuにuser引数追加・後方互換）／完了演出3関数を並列／progressをw.progressで使い回し | view描画・完了演出「明日は1周2日目・Cメニュー」不変・エラー0・DB復元 | ★きよむ合格 |
| D | ネスト取得4サイト | `src/lib/meals/queries.ts`, `src/lib/workout/logs.ts`, `src/lib/admin/daily.ts` | 親→子の2往復をネストselectで1往復に（/meals・/workout/history・/workout/today・/admin/daily）。子テーブルRLSは「self all」で空落ちしない | 前後フル画像一致・DB件数=画面件数・エラー0・DB復元 | ★きよむ合格 |
| #1 | /admin/messages（一覧N+1） | `src/lib/chat/queries.ts` | 会話ごと直列2クエリ×N → 会話間も会話内も並列（順序・値はmapで不変） | 型/lint・空状態描画OK。**dev に chat テーブル無し→実データ検証はprodのみ可**（きよむ「Aで入れておく」承認） | 実装済（prod確認推奨） |
| #3 | /admin/users/[id]/meals | `src/lib/admin/meals.ts` | 品目を親にネスト＋署名URL/FBを並列（4直列→2段） | 品目「もち麦ごはん…」表示・エラー0 | 実装済（dev確認OK） |
| #5 | /admin/courses/[c]/chapters/[ch] | `src/app/admin/courses/[courseId]/chapters/[chapterId]/page.tsx` | chapterガード後、course と lessons を並列 | 描画・エラー0 | 実装済（dev確認OK） |

## 重要な発見（次の人が知っておくこと）
- **#4 管理ホーム `/admin/page.tsx` は既に `Promise.all` 済＝改修不要**（棚卸しエージェントが過大評価。現物で訂正）。
- **管理画面は「既に並列化済で触る必要がないページ」が多い**（受講生一覧・学習・metrics・workout・monthly・月次詳細・お知らせ 等）。「全部やる」の実体は下記の残りグループだけ。
- **dev DB に chat（conversations/messages）テーブルが存在しない**（`to_regclass`=null）。/admin/messages は dev で常に空・N+1ループは dev で実行されない。

## 未着手（残り・優先順）
S1/棚卸しに基づく worklist。すべて dev で実データ検証できる（chat以外）。
- **#2 横断: `getCarteForAdmin`（`src/lib/workout/queries.ts:165付近`）が `user_profiles` を内部再取得** → ハブ/カルテ/マッチ の3ページで二重。関数に引数を足す or ネスト化で3ページ同時に効く。
- **#6 ハブ `/admin/users/[id]`**：users+user_profiles 直列＋上記重複。
- **#7 `/admin/requests`（詳細）**：req+users+carte 直列。
- **#8 `/admin/shipments`**：pending/shipped/total の count 3本が直列。
- **#9 軽微**：courses / videos / messages詳細 / goal-sheet / learning(受講生ハブ側) 等、独立2本の順次awaitを1段短縮。
- **受講生 E**：履歴・日別詳細の**画面外**写真のみ `loading="lazy"`（初回表示画像は除外）。
- **受講生 F**：ホーム `src/app/page.tsx` の beta後段 `Promise.all(getRecordStreak,getTodayWorkout)` を上の Promise.all(12) に合流。
- **やらない（きよむ確定）**：ホームの getUser 15回集約は影響大→東京化後に必要なら（保留）。

## 次の一手
1. 残り（#2→#6→#7→#8→#9→E→F）を優先順に、軽い検証（描画エラー0＋データ不変）で片付ける。
2. S2完了後：**devへdeploy → live TTFB を before→after で計測（3回中央値）** → 数字をきよむへ。
3. その後 S3（マスタ系キャッシュのみ・守りの3原則厳守・きよむ実機OK後に本番）。

## コミット/デプロイ状態
- ブランチ `feat/p3-home-beta` にコミット済み。**本番（main→Vercel prod）には未反映・未デプロイ**。
- 本番反映は deploy-isolated（feat→cherry-pick→main→push）で、きよむの合図後。
