# 引き継ぎ：筋肉塾9 の続き（前チャットの会話ログから復元）

前チャット「筋肉塾9」は **API 401（＝Claude Code のログイン切れ）で途中終了**した。
以下は、その会話ログの末尾から復元した「どこまでやったか／次に何をするか」。
着手前に `06_kinniku_juku_app/CLAUDE.md` と `.cursor/rules/` の正典も確認すること。

## ✅ 完了済み（前チャットで対応済み）
- **モンクモード／テキストレッスン対応**：`scripts/seed_monk_mode_READY.sql` の UPDATE を実行済み・本番デプロイ済み。`lessons.content_json` 反映を SELECT で確認済み。

## 🚧 途中だった作業（ここから再開）＝「0.はじめに」章の章ごと削除

### 目的
コース「限定ボディメイク完全ロードマップ」（全8章）のうち、
**「0. はじめに必ず視聴してください」章を、中の動画レッスン・章末試験ごと完全削除**し、
残り章の `sort_order` を詰める（**案X＝再採番**）。

### 確定済みの削除対象（前チャットで SELECT 確認済み）
- コース「限定ボディメイク完全ロードマップ」… **コース自体は残す**
- 削除する章：**id=`f4f8b6dc…03f`**（タイトル「0.はじめに必ず視聴して下さい」／`sort_order=2`）
- 章内 **7項目**：動画レッスン×6（①②③目標設定… ／ カロミル ／ バーンフィット 等）＋ **章末試験×1**
  - 章末試験は **`exams` テーブル**管理。L4 lesson「【テスト】目標設定の効果的なやり方」に `ON DELETE CASCADE` で紐付くため、lesson 削除で自動的に消える。

### 用意済みファイル（未コミット・実行はまだ）
- `scripts/inspect_chapter_to_delete.sql` … 削除対象の確認 SELECT
- `scripts/delete_intro_chapter_READY.sql` … 削除実行 SQL（**生成済み・まだ実行していない**）

### 次の手順（この順で・本番DBなので慎重に）
1. `inspect_chapter_to_delete.sql` の SELECT で、削除対象（章＋7項目）を**最終確認**し、結果を私（きよむ）に見せる。
2. 承認後、`delete_intro_chapter_READY.sql` を **Supabase SQL Editor** で実行。
   - Supabase project: `fqfsgkzyotvpcxmszkax`
3. 残り章の `sort_order` を詰め直す（**再採番**）。番号のずれ込みがないか確認。
4. 本番反映 → iPhone PWA（`demo-4month` デモアカウント）で実機確認。

### ⚠️ 注意
- 本番DBへの **DELETE**。**いきなり実行せず、必ず SELECT で対象を見せて承認を取ってから**。
- 削除後、コース一覧・章番号のずれ込みを確認。

---
**まず：`git status` と上記ファイルの有無を確認し、手順1の確認 SELECT を提示するところまで。そこで一旦止まること。**
