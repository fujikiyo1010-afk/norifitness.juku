-- =====================================================================
-- 2026-06-18: drop security_events (= 同日の判断見直し)
-- =====================================================================
--
-- 当初 #3-b で本人通知 + 管理者通知 + 履歴表示の 3 件をスコープに入れていたが、
-- 「受講生本人が変更するケースが大半 = 管理者通知は noise」 ときよむさん判断
-- (2026-06-18)。 結果、 履歴 UI も用途消滅 → table 自体不要。
--
-- 残るは本人宛セキュリティ通知メール (= src/lib/email/password-changed.ts) のみ。
--
-- 元 migration: 20260618000001_security_events.sql

drop table if exists public.security_events;
